import 'package:flutter/material.dart';

class Demo4 extends StatelessWidget {
  const Demo4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Temp4'),
      ),
      body: const Center(
        child: Text('Temp4'),
      ),
    );
  }
}
