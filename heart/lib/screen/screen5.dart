import 'package:flutter/material.dart';

class Demo5 extends StatelessWidget {
  const Demo5({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Temp5'),
      ),
      body: const Center(
        child: Text('Temp5'),
      ),
    );
  }
}
